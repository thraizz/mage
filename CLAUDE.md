In this project, we port XMage, a Magic: The Gathering game simulator to Go and Svelte.
We want to have everything that the java XMage version offers and more.
We want to have 100% rule coverage.
We want to build the MVP first before optimizing structure and architecture.
We want full test coverage.
We need all cards and spells supported.
Always use absolute paths for cd commands.
Never create final summary documents, only write down pending tasks in a compressed way.
