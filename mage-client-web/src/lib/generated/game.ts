// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.8.3
//   protoc               v6.33.1
// source: game.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import {
  type CallOptions,
  type ChannelCredentials,
  Client,
  type ClientOptions,
  type ClientReadableStream,
  type ClientUnaryCall,
  type handleServerStreamingCall,
  type handleUnaryCall,
  makeGenericClientConstructor,
  type Metadata,
  type ServiceError,
  type UntypedServiceImplementation,
} from "@grpc/grpc-js";

export enum UpdateType {
  STATE_CHANGE = 0,
  CARD_PLAYED = 1,
  PHASE_CHANGE = 2,
  DAMAGE_DEALT = 3,
  PLAYER_ACTION = 4,
  UNRECOGNIZED = -1,
}

export function updateTypeFromJSON(object: any): UpdateType {
  switch (object) {
    case 0:
    case "STATE_CHANGE":
      return UpdateType.STATE_CHANGE;
    case 1:
    case "CARD_PLAYED":
      return UpdateType.CARD_PLAYED;
    case 2:
    case "PHASE_CHANGE":
      return UpdateType.PHASE_CHANGE;
    case 3:
    case "DAMAGE_DEALT":
      return UpdateType.DAMAGE_DEALT;
    case 4:
    case "PLAYER_ACTION":
      return UpdateType.PLAYER_ACTION;
    case -1:
    case "UNRECOGNIZED":
    default:
      return UpdateType.UNRECOGNIZED;
  }
}

export function updateTypeToJSON(object: UpdateType): string {
  switch (object) {
    case UpdateType.STATE_CHANGE:
      return "STATE_CHANGE";
    case UpdateType.CARD_PLAYED:
      return "CARD_PLAYED";
    case UpdateType.PHASE_CHANGE:
      return "PHASE_CHANGE";
    case UpdateType.DAMAGE_DEALT:
      return "DAMAGE_DEALT";
    case UpdateType.PLAYER_ACTION:
      return "PLAYER_ACTION";
    case UpdateType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum ActionType {
  PLAY_CARD = 0,
  ACTIVATE_ABILITY = 1,
  PASS_PRIORITY = 2,
  DECLARE_ATTACKERS = 3,
  DECLARE_BLOCKERS = 4,
  CONCEDE = 5,
  UNRECOGNIZED = -1,
}

export function actionTypeFromJSON(object: any): ActionType {
  switch (object) {
    case 0:
    case "PLAY_CARD":
      return ActionType.PLAY_CARD;
    case 1:
    case "ACTIVATE_ABILITY":
      return ActionType.ACTIVATE_ABILITY;
    case 2:
    case "PASS_PRIORITY":
      return ActionType.PASS_PRIORITY;
    case 3:
    case "DECLARE_ATTACKERS":
      return ActionType.DECLARE_ATTACKERS;
    case 4:
    case "DECLARE_BLOCKERS":
      return ActionType.DECLARE_BLOCKERS;
    case 5:
    case "CONCEDE":
      return ActionType.CONCEDE;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ActionType.UNRECOGNIZED;
  }
}

export function actionTypeToJSON(object: ActionType): string {
  switch (object) {
    case ActionType.PLAY_CARD:
      return "PLAY_CARD";
    case ActionType.ACTIVATE_ABILITY:
      return "ACTIVATE_ABILITY";
    case ActionType.PASS_PRIORITY:
      return "PASS_PRIORITY";
    case ActionType.DECLARE_ATTACKERS:
      return "DECLARE_ATTACKERS";
    case ActionType.DECLARE_BLOCKERS:
      return "DECLARE_BLOCKERS";
    case ActionType.CONCEDE:
      return "CONCEDE";
    case ActionType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** Request to get game state */
export interface GameStateRequest {
  gameId: string;
  playerId: string;
}

/** Game state response */
export interface GameStateResponse {
  gameId: string;
  state?: GameState | undefined;
}

/** Game state structure */
export interface GameState {
  turnNumber: number;
  currentPhase: string;
  activePlayerId: string;
  priorityPlayerId: string;
  players: Player[];
  battlefield: Card[];
  stack: StackItem[];
}

/** Player information */
export interface Player {
  playerId: string;
  username: string;
  life: number;
  handSize: number;
  librarySize: number;
  graveyardSize: number;
  hand: Card[];
  graveyard: Card[];
}

/** Card representation */
export interface Card {
  cardId: string;
  name: string;
  manaCost: string;
  typeLine: string;
  tapped: boolean;
  power: number;
  toughness: number;
  controllerId: string;
  ownerId: string;
}

/** Stack item (spell or ability) */
export interface StackItem {
  itemId: string;
  sourceCardId: string;
  controllerId: string;
  description: string;
}

/** Real-time game update */
export interface GameUpdate {
  gameId: string;
  type: UpdateType;
  state?: GameState | undefined;
  message: string;
}

/** Game action request */
export interface GameActionRequest {
  gameId: string;
  playerId: string;
  actionType: ActionType;
  cardId: string;
  targetIds: string[];
  parameters: { [key: string]: string };
}

export interface GameActionRequest_ParametersEntry {
  key: string;
  value: string;
}

/** Game action response */
export interface GameActionResponse {
  success: boolean;
  message: string;
  newState?: GameState | undefined;
}

function createBaseGameStateRequest(): GameStateRequest {
  return { gameId: "", playerId: "" };
}

export const GameStateRequest: MessageFns<GameStateRequest> = {
  encode(message: GameStateRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.gameId !== "") {
      writer.uint32(10).string(message.gameId);
    }
    if (message.playerId !== "") {
      writer.uint32(18).string(message.playerId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GameStateRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGameStateRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.gameId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.playerId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GameStateRequest {
    return {
      gameId: isSet(object.gameId) ? globalThis.String(object.gameId) : "",
      playerId: isSet(object.playerId) ? globalThis.String(object.playerId) : "",
    };
  },

  toJSON(message: GameStateRequest): unknown {
    const obj: any = {};
    if (message.gameId !== "") {
      obj.gameId = message.gameId;
    }
    if (message.playerId !== "") {
      obj.playerId = message.playerId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GameStateRequest>, I>>(base?: I): GameStateRequest {
    return GameStateRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GameStateRequest>, I>>(object: I): GameStateRequest {
    const message = createBaseGameStateRequest();
    message.gameId = object.gameId ?? "";
    message.playerId = object.playerId ?? "";
    return message;
  },
};

function createBaseGameStateResponse(): GameStateResponse {
  return { gameId: "", state: undefined };
}

export const GameStateResponse: MessageFns<GameStateResponse> = {
  encode(message: GameStateResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.gameId !== "") {
      writer.uint32(10).string(message.gameId);
    }
    if (message.state !== undefined) {
      GameState.encode(message.state, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GameStateResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGameStateResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.gameId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.state = GameState.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GameStateResponse {
    return {
      gameId: isSet(object.gameId) ? globalThis.String(object.gameId) : "",
      state: isSet(object.state) ? GameState.fromJSON(object.state) : undefined,
    };
  },

  toJSON(message: GameStateResponse): unknown {
    const obj: any = {};
    if (message.gameId !== "") {
      obj.gameId = message.gameId;
    }
    if (message.state !== undefined) {
      obj.state = GameState.toJSON(message.state);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GameStateResponse>, I>>(base?: I): GameStateResponse {
    return GameStateResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GameStateResponse>, I>>(object: I): GameStateResponse {
    const message = createBaseGameStateResponse();
    message.gameId = object.gameId ?? "";
    message.state = (object.state !== undefined && object.state !== null)
      ? GameState.fromPartial(object.state)
      : undefined;
    return message;
  },
};

function createBaseGameState(): GameState {
  return {
    turnNumber: 0,
    currentPhase: "",
    activePlayerId: "",
    priorityPlayerId: "",
    players: [],
    battlefield: [],
    stack: [],
  };
}

export const GameState: MessageFns<GameState> = {
  encode(message: GameState, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.turnNumber !== 0) {
      writer.uint32(8).int32(message.turnNumber);
    }
    if (message.currentPhase !== "") {
      writer.uint32(18).string(message.currentPhase);
    }
    if (message.activePlayerId !== "") {
      writer.uint32(26).string(message.activePlayerId);
    }
    if (message.priorityPlayerId !== "") {
      writer.uint32(34).string(message.priorityPlayerId);
    }
    for (const v of message.players) {
      Player.encode(v!, writer.uint32(42).fork()).join();
    }
    for (const v of message.battlefield) {
      Card.encode(v!, writer.uint32(50).fork()).join();
    }
    for (const v of message.stack) {
      StackItem.encode(v!, writer.uint32(58).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GameState {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGameState();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.turnNumber = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.currentPhase = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.activePlayerId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.priorityPlayerId = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.players.push(Player.decode(reader, reader.uint32()));
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.battlefield.push(Card.decode(reader, reader.uint32()));
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.stack.push(StackItem.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GameState {
    return {
      turnNumber: isSet(object.turnNumber) ? globalThis.Number(object.turnNumber) : 0,
      currentPhase: isSet(object.currentPhase) ? globalThis.String(object.currentPhase) : "",
      activePlayerId: isSet(object.activePlayerId) ? globalThis.String(object.activePlayerId) : "",
      priorityPlayerId: isSet(object.priorityPlayerId) ? globalThis.String(object.priorityPlayerId) : "",
      players: globalThis.Array.isArray(object?.players) ? object.players.map((e: any) => Player.fromJSON(e)) : [],
      battlefield: globalThis.Array.isArray(object?.battlefield)
        ? object.battlefield.map((e: any) => Card.fromJSON(e))
        : [],
      stack: globalThis.Array.isArray(object?.stack) ? object.stack.map((e: any) => StackItem.fromJSON(e)) : [],
    };
  },

  toJSON(message: GameState): unknown {
    const obj: any = {};
    if (message.turnNumber !== 0) {
      obj.turnNumber = Math.round(message.turnNumber);
    }
    if (message.currentPhase !== "") {
      obj.currentPhase = message.currentPhase;
    }
    if (message.activePlayerId !== "") {
      obj.activePlayerId = message.activePlayerId;
    }
    if (message.priorityPlayerId !== "") {
      obj.priorityPlayerId = message.priorityPlayerId;
    }
    if (message.players?.length) {
      obj.players = message.players.map((e) => Player.toJSON(e));
    }
    if (message.battlefield?.length) {
      obj.battlefield = message.battlefield.map((e) => Card.toJSON(e));
    }
    if (message.stack?.length) {
      obj.stack = message.stack.map((e) => StackItem.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GameState>, I>>(base?: I): GameState {
    return GameState.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GameState>, I>>(object: I): GameState {
    const message = createBaseGameState();
    message.turnNumber = object.turnNumber ?? 0;
    message.currentPhase = object.currentPhase ?? "";
    message.activePlayerId = object.activePlayerId ?? "";
    message.priorityPlayerId = object.priorityPlayerId ?? "";
    message.players = object.players?.map((e) => Player.fromPartial(e)) || [];
    message.battlefield = object.battlefield?.map((e) => Card.fromPartial(e)) || [];
    message.stack = object.stack?.map((e) => StackItem.fromPartial(e)) || [];
    return message;
  },
};

function createBasePlayer(): Player {
  return {
    playerId: "",
    username: "",
    life: 0,
    handSize: 0,
    librarySize: 0,
    graveyardSize: 0,
    hand: [],
    graveyard: [],
  };
}

export const Player: MessageFns<Player> = {
  encode(message: Player, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.playerId !== "") {
      writer.uint32(10).string(message.playerId);
    }
    if (message.username !== "") {
      writer.uint32(18).string(message.username);
    }
    if (message.life !== 0) {
      writer.uint32(24).int32(message.life);
    }
    if (message.handSize !== 0) {
      writer.uint32(32).int32(message.handSize);
    }
    if (message.librarySize !== 0) {
      writer.uint32(40).int32(message.librarySize);
    }
    if (message.graveyardSize !== 0) {
      writer.uint32(48).int32(message.graveyardSize);
    }
    for (const v of message.hand) {
      Card.encode(v!, writer.uint32(58).fork()).join();
    }
    for (const v of message.graveyard) {
      Card.encode(v!, writer.uint32(66).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Player {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePlayer();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.playerId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.username = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.life = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.handSize = reader.int32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.librarySize = reader.int32();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.graveyardSize = reader.int32();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.hand.push(Card.decode(reader, reader.uint32()));
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.graveyard.push(Card.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Player {
    return {
      playerId: isSet(object.playerId) ? globalThis.String(object.playerId) : "",
      username: isSet(object.username) ? globalThis.String(object.username) : "",
      life: isSet(object.life) ? globalThis.Number(object.life) : 0,
      handSize: isSet(object.handSize) ? globalThis.Number(object.handSize) : 0,
      librarySize: isSet(object.librarySize) ? globalThis.Number(object.librarySize) : 0,
      graveyardSize: isSet(object.graveyardSize) ? globalThis.Number(object.graveyardSize) : 0,
      hand: globalThis.Array.isArray(object?.hand) ? object.hand.map((e: any) => Card.fromJSON(e)) : [],
      graveyard: globalThis.Array.isArray(object?.graveyard) ? object.graveyard.map((e: any) => Card.fromJSON(e)) : [],
    };
  },

  toJSON(message: Player): unknown {
    const obj: any = {};
    if (message.playerId !== "") {
      obj.playerId = message.playerId;
    }
    if (message.username !== "") {
      obj.username = message.username;
    }
    if (message.life !== 0) {
      obj.life = Math.round(message.life);
    }
    if (message.handSize !== 0) {
      obj.handSize = Math.round(message.handSize);
    }
    if (message.librarySize !== 0) {
      obj.librarySize = Math.round(message.librarySize);
    }
    if (message.graveyardSize !== 0) {
      obj.graveyardSize = Math.round(message.graveyardSize);
    }
    if (message.hand?.length) {
      obj.hand = message.hand.map((e) => Card.toJSON(e));
    }
    if (message.graveyard?.length) {
      obj.graveyard = message.graveyard.map((e) => Card.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Player>, I>>(base?: I): Player {
    return Player.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Player>, I>>(object: I): Player {
    const message = createBasePlayer();
    message.playerId = object.playerId ?? "";
    message.username = object.username ?? "";
    message.life = object.life ?? 0;
    message.handSize = object.handSize ?? 0;
    message.librarySize = object.librarySize ?? 0;
    message.graveyardSize = object.graveyardSize ?? 0;
    message.hand = object.hand?.map((e) => Card.fromPartial(e)) || [];
    message.graveyard = object.graveyard?.map((e) => Card.fromPartial(e)) || [];
    return message;
  },
};

function createBaseCard(): Card {
  return {
    cardId: "",
    name: "",
    manaCost: "",
    typeLine: "",
    tapped: false,
    power: 0,
    toughness: 0,
    controllerId: "",
    ownerId: "",
  };
}

export const Card: MessageFns<Card> = {
  encode(message: Card, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.cardId !== "") {
      writer.uint32(10).string(message.cardId);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.manaCost !== "") {
      writer.uint32(26).string(message.manaCost);
    }
    if (message.typeLine !== "") {
      writer.uint32(34).string(message.typeLine);
    }
    if (message.tapped !== false) {
      writer.uint32(40).bool(message.tapped);
    }
    if (message.power !== 0) {
      writer.uint32(48).int32(message.power);
    }
    if (message.toughness !== 0) {
      writer.uint32(56).int32(message.toughness);
    }
    if (message.controllerId !== "") {
      writer.uint32(66).string(message.controllerId);
    }
    if (message.ownerId !== "") {
      writer.uint32(74).string(message.ownerId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Card {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCard();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.cardId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.manaCost = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.typeLine = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.tapped = reader.bool();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.power = reader.int32();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.toughness = reader.int32();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.controllerId = reader.string();
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.ownerId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Card {
    return {
      cardId: isSet(object.cardId) ? globalThis.String(object.cardId) : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      manaCost: isSet(object.manaCost) ? globalThis.String(object.manaCost) : "",
      typeLine: isSet(object.typeLine) ? globalThis.String(object.typeLine) : "",
      tapped: isSet(object.tapped) ? globalThis.Boolean(object.tapped) : false,
      power: isSet(object.power) ? globalThis.Number(object.power) : 0,
      toughness: isSet(object.toughness) ? globalThis.Number(object.toughness) : 0,
      controllerId: isSet(object.controllerId) ? globalThis.String(object.controllerId) : "",
      ownerId: isSet(object.ownerId) ? globalThis.String(object.ownerId) : "",
    };
  },

  toJSON(message: Card): unknown {
    const obj: any = {};
    if (message.cardId !== "") {
      obj.cardId = message.cardId;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.manaCost !== "") {
      obj.manaCost = message.manaCost;
    }
    if (message.typeLine !== "") {
      obj.typeLine = message.typeLine;
    }
    if (message.tapped !== false) {
      obj.tapped = message.tapped;
    }
    if (message.power !== 0) {
      obj.power = Math.round(message.power);
    }
    if (message.toughness !== 0) {
      obj.toughness = Math.round(message.toughness);
    }
    if (message.controllerId !== "") {
      obj.controllerId = message.controllerId;
    }
    if (message.ownerId !== "") {
      obj.ownerId = message.ownerId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Card>, I>>(base?: I): Card {
    return Card.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Card>, I>>(object: I): Card {
    const message = createBaseCard();
    message.cardId = object.cardId ?? "";
    message.name = object.name ?? "";
    message.manaCost = object.manaCost ?? "";
    message.typeLine = object.typeLine ?? "";
    message.tapped = object.tapped ?? false;
    message.power = object.power ?? 0;
    message.toughness = object.toughness ?? 0;
    message.controllerId = object.controllerId ?? "";
    message.ownerId = object.ownerId ?? "";
    return message;
  },
};

function createBaseStackItem(): StackItem {
  return { itemId: "", sourceCardId: "", controllerId: "", description: "" };
}

export const StackItem: MessageFns<StackItem> = {
  encode(message: StackItem, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.itemId !== "") {
      writer.uint32(10).string(message.itemId);
    }
    if (message.sourceCardId !== "") {
      writer.uint32(18).string(message.sourceCardId);
    }
    if (message.controllerId !== "") {
      writer.uint32(26).string(message.controllerId);
    }
    if (message.description !== "") {
      writer.uint32(34).string(message.description);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StackItem {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStackItem();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.itemId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.sourceCardId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.controllerId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.description = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StackItem {
    return {
      itemId: isSet(object.itemId) ? globalThis.String(object.itemId) : "",
      sourceCardId: isSet(object.sourceCardId) ? globalThis.String(object.sourceCardId) : "",
      controllerId: isSet(object.controllerId) ? globalThis.String(object.controllerId) : "",
      description: isSet(object.description) ? globalThis.String(object.description) : "",
    };
  },

  toJSON(message: StackItem): unknown {
    const obj: any = {};
    if (message.itemId !== "") {
      obj.itemId = message.itemId;
    }
    if (message.sourceCardId !== "") {
      obj.sourceCardId = message.sourceCardId;
    }
    if (message.controllerId !== "") {
      obj.controllerId = message.controllerId;
    }
    if (message.description !== "") {
      obj.description = message.description;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StackItem>, I>>(base?: I): StackItem {
    return StackItem.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StackItem>, I>>(object: I): StackItem {
    const message = createBaseStackItem();
    message.itemId = object.itemId ?? "";
    message.sourceCardId = object.sourceCardId ?? "";
    message.controllerId = object.controllerId ?? "";
    message.description = object.description ?? "";
    return message;
  },
};

function createBaseGameUpdate(): GameUpdate {
  return { gameId: "", type: 0, state: undefined, message: "" };
}

export const GameUpdate: MessageFns<GameUpdate> = {
  encode(message: GameUpdate, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.gameId !== "") {
      writer.uint32(10).string(message.gameId);
    }
    if (message.type !== 0) {
      writer.uint32(16).int32(message.type);
    }
    if (message.state !== undefined) {
      GameState.encode(message.state, writer.uint32(26).fork()).join();
    }
    if (message.message !== "") {
      writer.uint32(34).string(message.message);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GameUpdate {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGameUpdate();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.gameId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.type = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.state = GameState.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.message = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GameUpdate {
    return {
      gameId: isSet(object.gameId) ? globalThis.String(object.gameId) : "",
      type: isSet(object.type) ? updateTypeFromJSON(object.type) : 0,
      state: isSet(object.state) ? GameState.fromJSON(object.state) : undefined,
      message: isSet(object.message) ? globalThis.String(object.message) : "",
    };
  },

  toJSON(message: GameUpdate): unknown {
    const obj: any = {};
    if (message.gameId !== "") {
      obj.gameId = message.gameId;
    }
    if (message.type !== 0) {
      obj.type = updateTypeToJSON(message.type);
    }
    if (message.state !== undefined) {
      obj.state = GameState.toJSON(message.state);
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GameUpdate>, I>>(base?: I): GameUpdate {
    return GameUpdate.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GameUpdate>, I>>(object: I): GameUpdate {
    const message = createBaseGameUpdate();
    message.gameId = object.gameId ?? "";
    message.type = object.type ?? 0;
    message.state = (object.state !== undefined && object.state !== null)
      ? GameState.fromPartial(object.state)
      : undefined;
    message.message = object.message ?? "";
    return message;
  },
};

function createBaseGameActionRequest(): GameActionRequest {
  return { gameId: "", playerId: "", actionType: 0, cardId: "", targetIds: [], parameters: {} };
}

export const GameActionRequest: MessageFns<GameActionRequest> = {
  encode(message: GameActionRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.gameId !== "") {
      writer.uint32(10).string(message.gameId);
    }
    if (message.playerId !== "") {
      writer.uint32(18).string(message.playerId);
    }
    if (message.actionType !== 0) {
      writer.uint32(24).int32(message.actionType);
    }
    if (message.cardId !== "") {
      writer.uint32(34).string(message.cardId);
    }
    for (const v of message.targetIds) {
      writer.uint32(42).string(v!);
    }
    Object.entries(message.parameters).forEach(([key, value]) => {
      GameActionRequest_ParametersEntry.encode({ key: key as any, value }, writer.uint32(50).fork()).join();
    });
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GameActionRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGameActionRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.gameId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.playerId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.actionType = reader.int32() as any;
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.cardId = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.targetIds.push(reader.string());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          const entry6 = GameActionRequest_ParametersEntry.decode(reader, reader.uint32());
          if (entry6.value !== undefined) {
            message.parameters[entry6.key] = entry6.value;
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GameActionRequest {
    return {
      gameId: isSet(object.gameId) ? globalThis.String(object.gameId) : "",
      playerId: isSet(object.playerId) ? globalThis.String(object.playerId) : "",
      actionType: isSet(object.actionType) ? actionTypeFromJSON(object.actionType) : 0,
      cardId: isSet(object.cardId) ? globalThis.String(object.cardId) : "",
      targetIds: globalThis.Array.isArray(object?.targetIds)
        ? object.targetIds.map((e: any) => globalThis.String(e))
        : [],
      parameters: isObject(object.parameters)
        ? Object.entries(object.parameters).reduce<{ [key: string]: string }>((acc, [key, value]) => {
          acc[key] = String(value);
          return acc;
        }, {})
        : {},
    };
  },

  toJSON(message: GameActionRequest): unknown {
    const obj: any = {};
    if (message.gameId !== "") {
      obj.gameId = message.gameId;
    }
    if (message.playerId !== "") {
      obj.playerId = message.playerId;
    }
    if (message.actionType !== 0) {
      obj.actionType = actionTypeToJSON(message.actionType);
    }
    if (message.cardId !== "") {
      obj.cardId = message.cardId;
    }
    if (message.targetIds?.length) {
      obj.targetIds = message.targetIds;
    }
    if (message.parameters) {
      const entries = Object.entries(message.parameters);
      if (entries.length > 0) {
        obj.parameters = {};
        entries.forEach(([k, v]) => {
          obj.parameters[k] = v;
        });
      }
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GameActionRequest>, I>>(base?: I): GameActionRequest {
    return GameActionRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GameActionRequest>, I>>(object: I): GameActionRequest {
    const message = createBaseGameActionRequest();
    message.gameId = object.gameId ?? "";
    message.playerId = object.playerId ?? "";
    message.actionType = object.actionType ?? 0;
    message.cardId = object.cardId ?? "";
    message.targetIds = object.targetIds?.map((e) => e) || [];
    message.parameters = Object.entries(object.parameters ?? {}).reduce<{ [key: string]: string }>(
      (acc, [key, value]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    return message;
  },
};

function createBaseGameActionRequest_ParametersEntry(): GameActionRequest_ParametersEntry {
  return { key: "", value: "" };
}

export const GameActionRequest_ParametersEntry: MessageFns<GameActionRequest_ParametersEntry> = {
  encode(message: GameActionRequest_ParametersEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GameActionRequest_ParametersEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGameActionRequest_ParametersEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GameActionRequest_ParametersEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: GameActionRequest_ParametersEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GameActionRequest_ParametersEntry>, I>>(
    base?: I,
  ): GameActionRequest_ParametersEntry {
    return GameActionRequest_ParametersEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GameActionRequest_ParametersEntry>, I>>(
    object: I,
  ): GameActionRequest_ParametersEntry {
    const message = createBaseGameActionRequest_ParametersEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseGameActionResponse(): GameActionResponse {
  return { success: false, message: "", newState: undefined };
}

export const GameActionResponse: MessageFns<GameActionResponse> = {
  encode(message: GameActionResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.success !== false) {
      writer.uint32(8).bool(message.success);
    }
    if (message.message !== "") {
      writer.uint32(18).string(message.message);
    }
    if (message.newState !== undefined) {
      GameState.encode(message.newState, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GameActionResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGameActionResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.message = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.newState = GameState.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GameActionResponse {
    return {
      success: isSet(object.success) ? globalThis.Boolean(object.success) : false,
      message: isSet(object.message) ? globalThis.String(object.message) : "",
      newState: isSet(object.newState) ? GameState.fromJSON(object.newState) : undefined,
    };
  },

  toJSON(message: GameActionResponse): unknown {
    const obj: any = {};
    if (message.success !== false) {
      obj.success = message.success;
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    if (message.newState !== undefined) {
      obj.newState = GameState.toJSON(message.newState);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GameActionResponse>, I>>(base?: I): GameActionResponse {
    return GameActionResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GameActionResponse>, I>>(object: I): GameActionResponse {
    const message = createBaseGameActionResponse();
    message.success = object.success ?? false;
    message.message = object.message ?? "";
    message.newState = (object.newState !== undefined && object.newState !== null)
      ? GameState.fromPartial(object.newState)
      : undefined;
    return message;
  },
};

/** Example game service for the MAGE multiplayer system */
export type GameServiceService = typeof GameServiceService;
export const GameServiceService = {
  /** Get current game state */
  getGameState: {
    path: "/mage.game.GameService/GetGameState",
    requestStream: false,
    responseStream: false,
    requestSerialize: (value: GameStateRequest): Buffer => Buffer.from(GameStateRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): GameStateRequest => GameStateRequest.decode(value),
    responseSerialize: (value: GameStateResponse): Buffer => Buffer.from(GameStateResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): GameStateResponse => GameStateResponse.decode(value),
  },
  /** Stream game updates in real-time */
  streamGameUpdates: {
    path: "/mage.game.GameService/StreamGameUpdates",
    requestStream: false,
    responseStream: true,
    requestSerialize: (value: GameStateRequest): Buffer => Buffer.from(GameStateRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): GameStateRequest => GameStateRequest.decode(value),
    responseSerialize: (value: GameUpdate): Buffer => Buffer.from(GameUpdate.encode(value).finish()),
    responseDeserialize: (value: Buffer): GameUpdate => GameUpdate.decode(value),
  },
  /** Perform a game action (play card, pass priority, etc.) */
  performAction: {
    path: "/mage.game.GameService/PerformAction",
    requestStream: false,
    responseStream: false,
    requestSerialize: (value: GameActionRequest): Buffer => Buffer.from(GameActionRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): GameActionRequest => GameActionRequest.decode(value),
    responseSerialize: (value: GameActionResponse): Buffer => Buffer.from(GameActionResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): GameActionResponse => GameActionResponse.decode(value),
  },
} as const;

export interface GameServiceServer extends UntypedServiceImplementation {
  /** Get current game state */
  getGameState: handleUnaryCall<GameStateRequest, GameStateResponse>;
  /** Stream game updates in real-time */
  streamGameUpdates: handleServerStreamingCall<GameStateRequest, GameUpdate>;
  /** Perform a game action (play card, pass priority, etc.) */
  performAction: handleUnaryCall<GameActionRequest, GameActionResponse>;
}

export interface GameServiceClient extends Client {
  /** Get current game state */
  getGameState(
    request: GameStateRequest,
    callback: (error: ServiceError | null, response: GameStateResponse) => void,
  ): ClientUnaryCall;
  getGameState(
    request: GameStateRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: GameStateResponse) => void,
  ): ClientUnaryCall;
  getGameState(
    request: GameStateRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: GameStateResponse) => void,
  ): ClientUnaryCall;
  /** Stream game updates in real-time */
  streamGameUpdates(request: GameStateRequest, options?: Partial<CallOptions>): ClientReadableStream<GameUpdate>;
  streamGameUpdates(
    request: GameStateRequest,
    metadata?: Metadata,
    options?: Partial<CallOptions>,
  ): ClientReadableStream<GameUpdate>;
  /** Perform a game action (play card, pass priority, etc.) */
  performAction(
    request: GameActionRequest,
    callback: (error: ServiceError | null, response: GameActionResponse) => void,
  ): ClientUnaryCall;
  performAction(
    request: GameActionRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: GameActionResponse) => void,
  ): ClientUnaryCall;
  performAction(
    request: GameActionRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: GameActionResponse) => void,
  ): ClientUnaryCall;
}

export const GameServiceClient = makeGenericClientConstructor(
  GameServiceService,
  "mage.game.GameService",
) as unknown as {
  new (address: string, credentials: ChannelCredentials, options?: Partial<ClientOptions>): GameServiceClient;
  service: typeof GameServiceService;
  serviceName: string;
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
