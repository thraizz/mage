// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.8.3
//   protoc               v6.33.1
// source: mage/v1/room.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Timestamp } from "../../google/protobuf/timestamp";
import { TableView, UserView } from "./models";

export interface ServerGetMainRoomIdRequest {
  sessionId: string;
}

export interface ServerGetMainRoomIdResponse {
  roomId: string;
}

export interface RoomGetUsersRequest {
  sessionId: string;
  roomId: string;
}

export interface RoomGetUsersResponse {
  users: UserView[];
}

export interface RoomGetFinishedMatchesRequest {
  sessionId: string;
  roomId: string;
}

export interface RoomGetFinishedMatchesResponse {
  finishedMatches: MatchView[];
}

export interface MatchView {
  gameType: string;
  deckType: string;
  players: string[];
  winner: string;
  durationMinutes: number;
  endTime?: Date | undefined;
  isTournament: boolean;
  tableName: string;
}

export interface RoomGetAllTablesRequest {
  sessionId: string;
  roomId: string;
}

export interface RoomGetAllTablesResponse {
  tables: TableView[];
}

export interface RoomGetTableByIdRequest {
  sessionId: string;
  roomId: string;
  tableId: string;
}

export interface RoomGetTableByIdResponse {
  table?: TableView | undefined;
}

export interface GetMatchHistoryRequest {
  sessionId: string;
  /** Default 50, max 100 */
  limit: number;
  /** For pagination */
  offset: number;
}

export interface MatchPlayerInfo {
  userId: number;
  username: string;
  /** Deck name or summary */
  deck: string;
  /** "win", "loss", "draw", "concede" */
  result: string;
}

export interface MatchHistoryEntry {
  id: number;
  gameId: string;
  tableId: string;
  tournamentId: string;
  players: MatchPlayerInfo[];
  gameType: string;
  startTime?: Date | undefined;
  endTime?: Date | undefined;
  durationSeconds: number;
  winnerId: number;
  winnerName: string;
  createdAt?: Date | undefined;
}

export interface GetMatchHistoryResponse {
  success: boolean;
  error: string;
  matches: MatchHistoryEntry[];
  /** Total matches for this user (for pagination) */
  totalCount: number;
}

export interface GetMatchByIdRequest {
  sessionId: string;
  matchId: number;
}

export interface GetMatchByIdResponse {
  success: boolean;
  error: string;
  match?:
    | MatchHistoryEntry
    | undefined;
  /** Optional: game log for replay */
  replayData: string;
}

function createBaseServerGetMainRoomIdRequest(): ServerGetMainRoomIdRequest {
  return { sessionId: "" };
}

export const ServerGetMainRoomIdRequest: MessageFns<ServerGetMainRoomIdRequest> = {
  encode(message: ServerGetMainRoomIdRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sessionId !== "") {
      writer.uint32(10).string(message.sessionId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ServerGetMainRoomIdRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseServerGetMainRoomIdRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ServerGetMainRoomIdRequest {
    return { sessionId: isSet(object.sessionId) ? globalThis.String(object.sessionId) : "" };
  },

  toJSON(message: ServerGetMainRoomIdRequest): unknown {
    const obj: any = {};
    if (message.sessionId !== "") {
      obj.sessionId = message.sessionId;
    }
    return obj;
  },

  create(base?: DeepPartial<ServerGetMainRoomIdRequest>): ServerGetMainRoomIdRequest {
    return ServerGetMainRoomIdRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ServerGetMainRoomIdRequest>): ServerGetMainRoomIdRequest {
    const message = createBaseServerGetMainRoomIdRequest();
    message.sessionId = object.sessionId ?? "";
    return message;
  },
};

function createBaseServerGetMainRoomIdResponse(): ServerGetMainRoomIdResponse {
  return { roomId: "" };
}

export const ServerGetMainRoomIdResponse: MessageFns<ServerGetMainRoomIdResponse> = {
  encode(message: ServerGetMainRoomIdResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.roomId !== "") {
      writer.uint32(10).string(message.roomId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ServerGetMainRoomIdResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseServerGetMainRoomIdResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.roomId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ServerGetMainRoomIdResponse {
    return { roomId: isSet(object.roomId) ? globalThis.String(object.roomId) : "" };
  },

  toJSON(message: ServerGetMainRoomIdResponse): unknown {
    const obj: any = {};
    if (message.roomId !== "") {
      obj.roomId = message.roomId;
    }
    return obj;
  },

  create(base?: DeepPartial<ServerGetMainRoomIdResponse>): ServerGetMainRoomIdResponse {
    return ServerGetMainRoomIdResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ServerGetMainRoomIdResponse>): ServerGetMainRoomIdResponse {
    const message = createBaseServerGetMainRoomIdResponse();
    message.roomId = object.roomId ?? "";
    return message;
  },
};

function createBaseRoomGetUsersRequest(): RoomGetUsersRequest {
  return { sessionId: "", roomId: "" };
}

export const RoomGetUsersRequest: MessageFns<RoomGetUsersRequest> = {
  encode(message: RoomGetUsersRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sessionId !== "") {
      writer.uint32(10).string(message.sessionId);
    }
    if (message.roomId !== "") {
      writer.uint32(18).string(message.roomId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RoomGetUsersRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRoomGetUsersRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.roomId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RoomGetUsersRequest {
    return {
      sessionId: isSet(object.sessionId) ? globalThis.String(object.sessionId) : "",
      roomId: isSet(object.roomId) ? globalThis.String(object.roomId) : "",
    };
  },

  toJSON(message: RoomGetUsersRequest): unknown {
    const obj: any = {};
    if (message.sessionId !== "") {
      obj.sessionId = message.sessionId;
    }
    if (message.roomId !== "") {
      obj.roomId = message.roomId;
    }
    return obj;
  },

  create(base?: DeepPartial<RoomGetUsersRequest>): RoomGetUsersRequest {
    return RoomGetUsersRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<RoomGetUsersRequest>): RoomGetUsersRequest {
    const message = createBaseRoomGetUsersRequest();
    message.sessionId = object.sessionId ?? "";
    message.roomId = object.roomId ?? "";
    return message;
  },
};

function createBaseRoomGetUsersResponse(): RoomGetUsersResponse {
  return { users: [] };
}

export const RoomGetUsersResponse: MessageFns<RoomGetUsersResponse> = {
  encode(message: RoomGetUsersResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.users) {
      UserView.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RoomGetUsersResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRoomGetUsersResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.users.push(UserView.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RoomGetUsersResponse {
    return { users: globalThis.Array.isArray(object?.users) ? object.users.map((e: any) => UserView.fromJSON(e)) : [] };
  },

  toJSON(message: RoomGetUsersResponse): unknown {
    const obj: any = {};
    if (message.users?.length) {
      obj.users = message.users.map((e) => UserView.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<RoomGetUsersResponse>): RoomGetUsersResponse {
    return RoomGetUsersResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<RoomGetUsersResponse>): RoomGetUsersResponse {
    const message = createBaseRoomGetUsersResponse();
    message.users = object.users?.map((e) => UserView.fromPartial(e)) || [];
    return message;
  },
};

function createBaseRoomGetFinishedMatchesRequest(): RoomGetFinishedMatchesRequest {
  return { sessionId: "", roomId: "" };
}

export const RoomGetFinishedMatchesRequest: MessageFns<RoomGetFinishedMatchesRequest> = {
  encode(message: RoomGetFinishedMatchesRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sessionId !== "") {
      writer.uint32(10).string(message.sessionId);
    }
    if (message.roomId !== "") {
      writer.uint32(18).string(message.roomId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RoomGetFinishedMatchesRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRoomGetFinishedMatchesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.roomId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RoomGetFinishedMatchesRequest {
    return {
      sessionId: isSet(object.sessionId) ? globalThis.String(object.sessionId) : "",
      roomId: isSet(object.roomId) ? globalThis.String(object.roomId) : "",
    };
  },

  toJSON(message: RoomGetFinishedMatchesRequest): unknown {
    const obj: any = {};
    if (message.sessionId !== "") {
      obj.sessionId = message.sessionId;
    }
    if (message.roomId !== "") {
      obj.roomId = message.roomId;
    }
    return obj;
  },

  create(base?: DeepPartial<RoomGetFinishedMatchesRequest>): RoomGetFinishedMatchesRequest {
    return RoomGetFinishedMatchesRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<RoomGetFinishedMatchesRequest>): RoomGetFinishedMatchesRequest {
    const message = createBaseRoomGetFinishedMatchesRequest();
    message.sessionId = object.sessionId ?? "";
    message.roomId = object.roomId ?? "";
    return message;
  },
};

function createBaseRoomGetFinishedMatchesResponse(): RoomGetFinishedMatchesResponse {
  return { finishedMatches: [] };
}

export const RoomGetFinishedMatchesResponse: MessageFns<RoomGetFinishedMatchesResponse> = {
  encode(message: RoomGetFinishedMatchesResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.finishedMatches) {
      MatchView.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RoomGetFinishedMatchesResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRoomGetFinishedMatchesResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.finishedMatches.push(MatchView.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RoomGetFinishedMatchesResponse {
    return {
      finishedMatches: globalThis.Array.isArray(object?.finishedMatches)
        ? object.finishedMatches.map((e: any) => MatchView.fromJSON(e))
        : [],
    };
  },

  toJSON(message: RoomGetFinishedMatchesResponse): unknown {
    const obj: any = {};
    if (message.finishedMatches?.length) {
      obj.finishedMatches = message.finishedMatches.map((e) => MatchView.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<RoomGetFinishedMatchesResponse>): RoomGetFinishedMatchesResponse {
    return RoomGetFinishedMatchesResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<RoomGetFinishedMatchesResponse>): RoomGetFinishedMatchesResponse {
    const message = createBaseRoomGetFinishedMatchesResponse();
    message.finishedMatches = object.finishedMatches?.map((e) => MatchView.fromPartial(e)) || [];
    return message;
  },
};

function createBaseMatchView(): MatchView {
  return {
    gameType: "",
    deckType: "",
    players: [],
    winner: "",
    durationMinutes: 0,
    endTime: undefined,
    isTournament: false,
    tableName: "",
  };
}

export const MatchView: MessageFns<MatchView> = {
  encode(message: MatchView, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.gameType !== "") {
      writer.uint32(10).string(message.gameType);
    }
    if (message.deckType !== "") {
      writer.uint32(18).string(message.deckType);
    }
    for (const v of message.players) {
      writer.uint32(26).string(v!);
    }
    if (message.winner !== "") {
      writer.uint32(34).string(message.winner);
    }
    if (message.durationMinutes !== 0) {
      writer.uint32(40).int32(message.durationMinutes);
    }
    if (message.endTime !== undefined) {
      Timestamp.encode(toTimestamp(message.endTime), writer.uint32(50).fork()).join();
    }
    if (message.isTournament !== false) {
      writer.uint32(56).bool(message.isTournament);
    }
    if (message.tableName !== "") {
      writer.uint32(66).string(message.tableName);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MatchView {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMatchView();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.gameType = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.deckType = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.players.push(reader.string());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.winner = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.durationMinutes = reader.int32();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.endTime = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.isTournament = reader.bool();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.tableName = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MatchView {
    return {
      gameType: isSet(object.gameType) ? globalThis.String(object.gameType) : "",
      deckType: isSet(object.deckType) ? globalThis.String(object.deckType) : "",
      players: globalThis.Array.isArray(object?.players) ? object.players.map((e: any) => globalThis.String(e)) : [],
      winner: isSet(object.winner) ? globalThis.String(object.winner) : "",
      durationMinutes: isSet(object.durationMinutes) ? globalThis.Number(object.durationMinutes) : 0,
      endTime: isSet(object.endTime) ? fromJsonTimestamp(object.endTime) : undefined,
      isTournament: isSet(object.isTournament) ? globalThis.Boolean(object.isTournament) : false,
      tableName: isSet(object.tableName) ? globalThis.String(object.tableName) : "",
    };
  },

  toJSON(message: MatchView): unknown {
    const obj: any = {};
    if (message.gameType !== "") {
      obj.gameType = message.gameType;
    }
    if (message.deckType !== "") {
      obj.deckType = message.deckType;
    }
    if (message.players?.length) {
      obj.players = message.players;
    }
    if (message.winner !== "") {
      obj.winner = message.winner;
    }
    if (message.durationMinutes !== 0) {
      obj.durationMinutes = Math.round(message.durationMinutes);
    }
    if (message.endTime !== undefined) {
      obj.endTime = message.endTime.toISOString();
    }
    if (message.isTournament !== false) {
      obj.isTournament = message.isTournament;
    }
    if (message.tableName !== "") {
      obj.tableName = message.tableName;
    }
    return obj;
  },

  create(base?: DeepPartial<MatchView>): MatchView {
    return MatchView.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MatchView>): MatchView {
    const message = createBaseMatchView();
    message.gameType = object.gameType ?? "";
    message.deckType = object.deckType ?? "";
    message.players = object.players?.map((e) => e) || [];
    message.winner = object.winner ?? "";
    message.durationMinutes = object.durationMinutes ?? 0;
    message.endTime = object.endTime ?? undefined;
    message.isTournament = object.isTournament ?? false;
    message.tableName = object.tableName ?? "";
    return message;
  },
};

function createBaseRoomGetAllTablesRequest(): RoomGetAllTablesRequest {
  return { sessionId: "", roomId: "" };
}

export const RoomGetAllTablesRequest: MessageFns<RoomGetAllTablesRequest> = {
  encode(message: RoomGetAllTablesRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sessionId !== "") {
      writer.uint32(10).string(message.sessionId);
    }
    if (message.roomId !== "") {
      writer.uint32(18).string(message.roomId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RoomGetAllTablesRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRoomGetAllTablesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.roomId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RoomGetAllTablesRequest {
    return {
      sessionId: isSet(object.sessionId) ? globalThis.String(object.sessionId) : "",
      roomId: isSet(object.roomId) ? globalThis.String(object.roomId) : "",
    };
  },

  toJSON(message: RoomGetAllTablesRequest): unknown {
    const obj: any = {};
    if (message.sessionId !== "") {
      obj.sessionId = message.sessionId;
    }
    if (message.roomId !== "") {
      obj.roomId = message.roomId;
    }
    return obj;
  },

  create(base?: DeepPartial<RoomGetAllTablesRequest>): RoomGetAllTablesRequest {
    return RoomGetAllTablesRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<RoomGetAllTablesRequest>): RoomGetAllTablesRequest {
    const message = createBaseRoomGetAllTablesRequest();
    message.sessionId = object.sessionId ?? "";
    message.roomId = object.roomId ?? "";
    return message;
  },
};

function createBaseRoomGetAllTablesResponse(): RoomGetAllTablesResponse {
  return { tables: [] };
}

export const RoomGetAllTablesResponse: MessageFns<RoomGetAllTablesResponse> = {
  encode(message: RoomGetAllTablesResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.tables) {
      TableView.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RoomGetAllTablesResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRoomGetAllTablesResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.tables.push(TableView.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RoomGetAllTablesResponse {
    return {
      tables: globalThis.Array.isArray(object?.tables) ? object.tables.map((e: any) => TableView.fromJSON(e)) : [],
    };
  },

  toJSON(message: RoomGetAllTablesResponse): unknown {
    const obj: any = {};
    if (message.tables?.length) {
      obj.tables = message.tables.map((e) => TableView.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<RoomGetAllTablesResponse>): RoomGetAllTablesResponse {
    return RoomGetAllTablesResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<RoomGetAllTablesResponse>): RoomGetAllTablesResponse {
    const message = createBaseRoomGetAllTablesResponse();
    message.tables = object.tables?.map((e) => TableView.fromPartial(e)) || [];
    return message;
  },
};

function createBaseRoomGetTableByIdRequest(): RoomGetTableByIdRequest {
  return { sessionId: "", roomId: "", tableId: "" };
}

export const RoomGetTableByIdRequest: MessageFns<RoomGetTableByIdRequest> = {
  encode(message: RoomGetTableByIdRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sessionId !== "") {
      writer.uint32(10).string(message.sessionId);
    }
    if (message.roomId !== "") {
      writer.uint32(18).string(message.roomId);
    }
    if (message.tableId !== "") {
      writer.uint32(26).string(message.tableId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RoomGetTableByIdRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRoomGetTableByIdRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.roomId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.tableId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RoomGetTableByIdRequest {
    return {
      sessionId: isSet(object.sessionId) ? globalThis.String(object.sessionId) : "",
      roomId: isSet(object.roomId) ? globalThis.String(object.roomId) : "",
      tableId: isSet(object.tableId) ? globalThis.String(object.tableId) : "",
    };
  },

  toJSON(message: RoomGetTableByIdRequest): unknown {
    const obj: any = {};
    if (message.sessionId !== "") {
      obj.sessionId = message.sessionId;
    }
    if (message.roomId !== "") {
      obj.roomId = message.roomId;
    }
    if (message.tableId !== "") {
      obj.tableId = message.tableId;
    }
    return obj;
  },

  create(base?: DeepPartial<RoomGetTableByIdRequest>): RoomGetTableByIdRequest {
    return RoomGetTableByIdRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<RoomGetTableByIdRequest>): RoomGetTableByIdRequest {
    const message = createBaseRoomGetTableByIdRequest();
    message.sessionId = object.sessionId ?? "";
    message.roomId = object.roomId ?? "";
    message.tableId = object.tableId ?? "";
    return message;
  },
};

function createBaseRoomGetTableByIdResponse(): RoomGetTableByIdResponse {
  return { table: undefined };
}

export const RoomGetTableByIdResponse: MessageFns<RoomGetTableByIdResponse> = {
  encode(message: RoomGetTableByIdResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.table !== undefined) {
      TableView.encode(message.table, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RoomGetTableByIdResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRoomGetTableByIdResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.table = TableView.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RoomGetTableByIdResponse {
    return { table: isSet(object.table) ? TableView.fromJSON(object.table) : undefined };
  },

  toJSON(message: RoomGetTableByIdResponse): unknown {
    const obj: any = {};
    if (message.table !== undefined) {
      obj.table = TableView.toJSON(message.table);
    }
    return obj;
  },

  create(base?: DeepPartial<RoomGetTableByIdResponse>): RoomGetTableByIdResponse {
    return RoomGetTableByIdResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<RoomGetTableByIdResponse>): RoomGetTableByIdResponse {
    const message = createBaseRoomGetTableByIdResponse();
    message.table = (object.table !== undefined && object.table !== null)
      ? TableView.fromPartial(object.table)
      : undefined;
    return message;
  },
};

function createBaseGetMatchHistoryRequest(): GetMatchHistoryRequest {
  return { sessionId: "", limit: 0, offset: 0 };
}

export const GetMatchHistoryRequest: MessageFns<GetMatchHistoryRequest> = {
  encode(message: GetMatchHistoryRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sessionId !== "") {
      writer.uint32(10).string(message.sessionId);
    }
    if (message.limit !== 0) {
      writer.uint32(16).int32(message.limit);
    }
    if (message.offset !== 0) {
      writer.uint32(24).int32(message.offset);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetMatchHistoryRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetMatchHistoryRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.limit = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.offset = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetMatchHistoryRequest {
    return {
      sessionId: isSet(object.sessionId) ? globalThis.String(object.sessionId) : "",
      limit: isSet(object.limit) ? globalThis.Number(object.limit) : 0,
      offset: isSet(object.offset) ? globalThis.Number(object.offset) : 0,
    };
  },

  toJSON(message: GetMatchHistoryRequest): unknown {
    const obj: any = {};
    if (message.sessionId !== "") {
      obj.sessionId = message.sessionId;
    }
    if (message.limit !== 0) {
      obj.limit = Math.round(message.limit);
    }
    if (message.offset !== 0) {
      obj.offset = Math.round(message.offset);
    }
    return obj;
  },

  create(base?: DeepPartial<GetMatchHistoryRequest>): GetMatchHistoryRequest {
    return GetMatchHistoryRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetMatchHistoryRequest>): GetMatchHistoryRequest {
    const message = createBaseGetMatchHistoryRequest();
    message.sessionId = object.sessionId ?? "";
    message.limit = object.limit ?? 0;
    message.offset = object.offset ?? 0;
    return message;
  },
};

function createBaseMatchPlayerInfo(): MatchPlayerInfo {
  return { userId: 0, username: "", deck: "", result: "" };
}

export const MatchPlayerInfo: MessageFns<MatchPlayerInfo> = {
  encode(message: MatchPlayerInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.userId !== 0) {
      writer.uint32(8).int64(message.userId);
    }
    if (message.username !== "") {
      writer.uint32(18).string(message.username);
    }
    if (message.deck !== "") {
      writer.uint32(26).string(message.deck);
    }
    if (message.result !== "") {
      writer.uint32(34).string(message.result);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MatchPlayerInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMatchPlayerInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.userId = longToNumber(reader.int64());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.username = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.deck = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.result = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MatchPlayerInfo {
    return {
      userId: isSet(object.userId) ? globalThis.Number(object.userId) : 0,
      username: isSet(object.username) ? globalThis.String(object.username) : "",
      deck: isSet(object.deck) ? globalThis.String(object.deck) : "",
      result: isSet(object.result) ? globalThis.String(object.result) : "",
    };
  },

  toJSON(message: MatchPlayerInfo): unknown {
    const obj: any = {};
    if (message.userId !== 0) {
      obj.userId = Math.round(message.userId);
    }
    if (message.username !== "") {
      obj.username = message.username;
    }
    if (message.deck !== "") {
      obj.deck = message.deck;
    }
    if (message.result !== "") {
      obj.result = message.result;
    }
    return obj;
  },

  create(base?: DeepPartial<MatchPlayerInfo>): MatchPlayerInfo {
    return MatchPlayerInfo.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MatchPlayerInfo>): MatchPlayerInfo {
    const message = createBaseMatchPlayerInfo();
    message.userId = object.userId ?? 0;
    message.username = object.username ?? "";
    message.deck = object.deck ?? "";
    message.result = object.result ?? "";
    return message;
  },
};

function createBaseMatchHistoryEntry(): MatchHistoryEntry {
  return {
    id: 0,
    gameId: "",
    tableId: "",
    tournamentId: "",
    players: [],
    gameType: "",
    startTime: undefined,
    endTime: undefined,
    durationSeconds: 0,
    winnerId: 0,
    winnerName: "",
    createdAt: undefined,
  };
}

export const MatchHistoryEntry: MessageFns<MatchHistoryEntry> = {
  encode(message: MatchHistoryEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== 0) {
      writer.uint32(8).int64(message.id);
    }
    if (message.gameId !== "") {
      writer.uint32(18).string(message.gameId);
    }
    if (message.tableId !== "") {
      writer.uint32(26).string(message.tableId);
    }
    if (message.tournamentId !== "") {
      writer.uint32(34).string(message.tournamentId);
    }
    for (const v of message.players) {
      MatchPlayerInfo.encode(v!, writer.uint32(42).fork()).join();
    }
    if (message.gameType !== "") {
      writer.uint32(50).string(message.gameType);
    }
    if (message.startTime !== undefined) {
      Timestamp.encode(toTimestamp(message.startTime), writer.uint32(58).fork()).join();
    }
    if (message.endTime !== undefined) {
      Timestamp.encode(toTimestamp(message.endTime), writer.uint32(66).fork()).join();
    }
    if (message.durationSeconds !== 0) {
      writer.uint32(72).int32(message.durationSeconds);
    }
    if (message.winnerId !== 0) {
      writer.uint32(80).int64(message.winnerId);
    }
    if (message.winnerName !== "") {
      writer.uint32(90).string(message.winnerName);
    }
    if (message.createdAt !== undefined) {
      Timestamp.encode(toTimestamp(message.createdAt), writer.uint32(98).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MatchHistoryEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMatchHistoryEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.id = longToNumber(reader.int64());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.gameId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.tableId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.tournamentId = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.players.push(MatchPlayerInfo.decode(reader, reader.uint32()));
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.gameType = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.startTime = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.endTime = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.durationSeconds = reader.int32();
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.winnerId = longToNumber(reader.int64());
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.winnerName = reader.string();
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.createdAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MatchHistoryEntry {
    return {
      id: isSet(object.id) ? globalThis.Number(object.id) : 0,
      gameId: isSet(object.gameId) ? globalThis.String(object.gameId) : "",
      tableId: isSet(object.tableId) ? globalThis.String(object.tableId) : "",
      tournamentId: isSet(object.tournamentId) ? globalThis.String(object.tournamentId) : "",
      players: globalThis.Array.isArray(object?.players)
        ? object.players.map((e: any) => MatchPlayerInfo.fromJSON(e))
        : [],
      gameType: isSet(object.gameType) ? globalThis.String(object.gameType) : "",
      startTime: isSet(object.startTime) ? fromJsonTimestamp(object.startTime) : undefined,
      endTime: isSet(object.endTime) ? fromJsonTimestamp(object.endTime) : undefined,
      durationSeconds: isSet(object.durationSeconds) ? globalThis.Number(object.durationSeconds) : 0,
      winnerId: isSet(object.winnerId) ? globalThis.Number(object.winnerId) : 0,
      winnerName: isSet(object.winnerName) ? globalThis.String(object.winnerName) : "",
      createdAt: isSet(object.createdAt) ? fromJsonTimestamp(object.createdAt) : undefined,
    };
  },

  toJSON(message: MatchHistoryEntry): unknown {
    const obj: any = {};
    if (message.id !== 0) {
      obj.id = Math.round(message.id);
    }
    if (message.gameId !== "") {
      obj.gameId = message.gameId;
    }
    if (message.tableId !== "") {
      obj.tableId = message.tableId;
    }
    if (message.tournamentId !== "") {
      obj.tournamentId = message.tournamentId;
    }
    if (message.players?.length) {
      obj.players = message.players.map((e) => MatchPlayerInfo.toJSON(e));
    }
    if (message.gameType !== "") {
      obj.gameType = message.gameType;
    }
    if (message.startTime !== undefined) {
      obj.startTime = message.startTime.toISOString();
    }
    if (message.endTime !== undefined) {
      obj.endTime = message.endTime.toISOString();
    }
    if (message.durationSeconds !== 0) {
      obj.durationSeconds = Math.round(message.durationSeconds);
    }
    if (message.winnerId !== 0) {
      obj.winnerId = Math.round(message.winnerId);
    }
    if (message.winnerName !== "") {
      obj.winnerName = message.winnerName;
    }
    if (message.createdAt !== undefined) {
      obj.createdAt = message.createdAt.toISOString();
    }
    return obj;
  },

  create(base?: DeepPartial<MatchHistoryEntry>): MatchHistoryEntry {
    return MatchHistoryEntry.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MatchHistoryEntry>): MatchHistoryEntry {
    const message = createBaseMatchHistoryEntry();
    message.id = object.id ?? 0;
    message.gameId = object.gameId ?? "";
    message.tableId = object.tableId ?? "";
    message.tournamentId = object.tournamentId ?? "";
    message.players = object.players?.map((e) => MatchPlayerInfo.fromPartial(e)) || [];
    message.gameType = object.gameType ?? "";
    message.startTime = object.startTime ?? undefined;
    message.endTime = object.endTime ?? undefined;
    message.durationSeconds = object.durationSeconds ?? 0;
    message.winnerId = object.winnerId ?? 0;
    message.winnerName = object.winnerName ?? "";
    message.createdAt = object.createdAt ?? undefined;
    return message;
  },
};

function createBaseGetMatchHistoryResponse(): GetMatchHistoryResponse {
  return { success: false, error: "", matches: [], totalCount: 0 };
}

export const GetMatchHistoryResponse: MessageFns<GetMatchHistoryResponse> = {
  encode(message: GetMatchHistoryResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.success !== false) {
      writer.uint32(8).bool(message.success);
    }
    if (message.error !== "") {
      writer.uint32(18).string(message.error);
    }
    for (const v of message.matches) {
      MatchHistoryEntry.encode(v!, writer.uint32(26).fork()).join();
    }
    if (message.totalCount !== 0) {
      writer.uint32(32).int32(message.totalCount);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetMatchHistoryResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetMatchHistoryResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.error = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.matches.push(MatchHistoryEntry.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.totalCount = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetMatchHistoryResponse {
    return {
      success: isSet(object.success) ? globalThis.Boolean(object.success) : false,
      error: isSet(object.error) ? globalThis.String(object.error) : "",
      matches: globalThis.Array.isArray(object?.matches)
        ? object.matches.map((e: any) => MatchHistoryEntry.fromJSON(e))
        : [],
      totalCount: isSet(object.totalCount) ? globalThis.Number(object.totalCount) : 0,
    };
  },

  toJSON(message: GetMatchHistoryResponse): unknown {
    const obj: any = {};
    if (message.success !== false) {
      obj.success = message.success;
    }
    if (message.error !== "") {
      obj.error = message.error;
    }
    if (message.matches?.length) {
      obj.matches = message.matches.map((e) => MatchHistoryEntry.toJSON(e));
    }
    if (message.totalCount !== 0) {
      obj.totalCount = Math.round(message.totalCount);
    }
    return obj;
  },

  create(base?: DeepPartial<GetMatchHistoryResponse>): GetMatchHistoryResponse {
    return GetMatchHistoryResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetMatchHistoryResponse>): GetMatchHistoryResponse {
    const message = createBaseGetMatchHistoryResponse();
    message.success = object.success ?? false;
    message.error = object.error ?? "";
    message.matches = object.matches?.map((e) => MatchHistoryEntry.fromPartial(e)) || [];
    message.totalCount = object.totalCount ?? 0;
    return message;
  },
};

function createBaseGetMatchByIdRequest(): GetMatchByIdRequest {
  return { sessionId: "", matchId: 0 };
}

export const GetMatchByIdRequest: MessageFns<GetMatchByIdRequest> = {
  encode(message: GetMatchByIdRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sessionId !== "") {
      writer.uint32(10).string(message.sessionId);
    }
    if (message.matchId !== 0) {
      writer.uint32(16).int64(message.matchId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetMatchByIdRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetMatchByIdRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.matchId = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetMatchByIdRequest {
    return {
      sessionId: isSet(object.sessionId) ? globalThis.String(object.sessionId) : "",
      matchId: isSet(object.matchId) ? globalThis.Number(object.matchId) : 0,
    };
  },

  toJSON(message: GetMatchByIdRequest): unknown {
    const obj: any = {};
    if (message.sessionId !== "") {
      obj.sessionId = message.sessionId;
    }
    if (message.matchId !== 0) {
      obj.matchId = Math.round(message.matchId);
    }
    return obj;
  },

  create(base?: DeepPartial<GetMatchByIdRequest>): GetMatchByIdRequest {
    return GetMatchByIdRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetMatchByIdRequest>): GetMatchByIdRequest {
    const message = createBaseGetMatchByIdRequest();
    message.sessionId = object.sessionId ?? "";
    message.matchId = object.matchId ?? 0;
    return message;
  },
};

function createBaseGetMatchByIdResponse(): GetMatchByIdResponse {
  return { success: false, error: "", match: undefined, replayData: "" };
}

export const GetMatchByIdResponse: MessageFns<GetMatchByIdResponse> = {
  encode(message: GetMatchByIdResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.success !== false) {
      writer.uint32(8).bool(message.success);
    }
    if (message.error !== "") {
      writer.uint32(18).string(message.error);
    }
    if (message.match !== undefined) {
      MatchHistoryEntry.encode(message.match, writer.uint32(26).fork()).join();
    }
    if (message.replayData !== "") {
      writer.uint32(34).string(message.replayData);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetMatchByIdResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetMatchByIdResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.error = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.match = MatchHistoryEntry.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.replayData = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetMatchByIdResponse {
    return {
      success: isSet(object.success) ? globalThis.Boolean(object.success) : false,
      error: isSet(object.error) ? globalThis.String(object.error) : "",
      match: isSet(object.match) ? MatchHistoryEntry.fromJSON(object.match) : undefined,
      replayData: isSet(object.replayData) ? globalThis.String(object.replayData) : "",
    };
  },

  toJSON(message: GetMatchByIdResponse): unknown {
    const obj: any = {};
    if (message.success !== false) {
      obj.success = message.success;
    }
    if (message.error !== "") {
      obj.error = message.error;
    }
    if (message.match !== undefined) {
      obj.match = MatchHistoryEntry.toJSON(message.match);
    }
    if (message.replayData !== "") {
      obj.replayData = message.replayData;
    }
    return obj;
  },

  create(base?: DeepPartial<GetMatchByIdResponse>): GetMatchByIdResponse {
    return GetMatchByIdResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetMatchByIdResponse>): GetMatchByIdResponse {
    const message = createBaseGetMatchByIdResponse();
    message.success = object.success ?? false;
    message.error = object.error ?? "";
    message.match = (object.match !== undefined && object.match !== null)
      ? MatchHistoryEntry.fromPartial(object.match)
      : undefined;
    message.replayData = object.replayData ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
