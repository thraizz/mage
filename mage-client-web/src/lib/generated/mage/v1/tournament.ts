// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.8.3
//   protoc               v6.33.2
// source: mage/v1/tournament.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { TournamentView } from "./models";

export interface TournamentJoinRequest {
  sessionId: string;
  tournamentId: string;
}

export interface TournamentJoinResponse {
  success: boolean;
  error: string;
}

export interface TournamentStartRequest {
  sessionId: string;
  roomId: string;
  tournamentId: string;
}

export interface TournamentStartResponse {
  success: boolean;
  error: string;
}

export interface TournamentQuitRequest {
  sessionId: string;
  tournamentId: string;
}

export interface TournamentQuitResponse {
  success: boolean;
  error: string;
}

export interface TournamentFindByIdRequest {
  sessionId: string;
  tournamentId: string;
}

export interface TournamentFindByIdResponse {
  tournament?: TournamentView | undefined;
}

function createBaseTournamentJoinRequest(): TournamentJoinRequest {
  return { sessionId: "", tournamentId: "" };
}

export const TournamentJoinRequest: MessageFns<TournamentJoinRequest> = {
  encode(message: TournamentJoinRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sessionId !== "") {
      writer.uint32(10).string(message.sessionId);
    }
    if (message.tournamentId !== "") {
      writer.uint32(18).string(message.tournamentId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TournamentJoinRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTournamentJoinRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.tournamentId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TournamentJoinRequest {
    return {
      sessionId: isSet(object.sessionId) ? globalThis.String(object.sessionId) : "",
      tournamentId: isSet(object.tournamentId) ? globalThis.String(object.tournamentId) : "",
    };
  },

  toJSON(message: TournamentJoinRequest): unknown {
    const obj: any = {};
    if (message.sessionId !== "") {
      obj.sessionId = message.sessionId;
    }
    if (message.tournamentId !== "") {
      obj.tournamentId = message.tournamentId;
    }
    return obj;
  },

  create(base?: DeepPartial<TournamentJoinRequest>): TournamentJoinRequest {
    return TournamentJoinRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<TournamentJoinRequest>): TournamentJoinRequest {
    const message = createBaseTournamentJoinRequest();
    message.sessionId = object.sessionId ?? "";
    message.tournamentId = object.tournamentId ?? "";
    return message;
  },
};

function createBaseTournamentJoinResponse(): TournamentJoinResponse {
  return { success: false, error: "" };
}

export const TournamentJoinResponse: MessageFns<TournamentJoinResponse> = {
  encode(message: TournamentJoinResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.success !== false) {
      writer.uint32(8).bool(message.success);
    }
    if (message.error !== "") {
      writer.uint32(18).string(message.error);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TournamentJoinResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTournamentJoinResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.error = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TournamentJoinResponse {
    return {
      success: isSet(object.success) ? globalThis.Boolean(object.success) : false,
      error: isSet(object.error) ? globalThis.String(object.error) : "",
    };
  },

  toJSON(message: TournamentJoinResponse): unknown {
    const obj: any = {};
    if (message.success !== false) {
      obj.success = message.success;
    }
    if (message.error !== "") {
      obj.error = message.error;
    }
    return obj;
  },

  create(base?: DeepPartial<TournamentJoinResponse>): TournamentJoinResponse {
    return TournamentJoinResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<TournamentJoinResponse>): TournamentJoinResponse {
    const message = createBaseTournamentJoinResponse();
    message.success = object.success ?? false;
    message.error = object.error ?? "";
    return message;
  },
};

function createBaseTournamentStartRequest(): TournamentStartRequest {
  return { sessionId: "", roomId: "", tournamentId: "" };
}

export const TournamentStartRequest: MessageFns<TournamentStartRequest> = {
  encode(message: TournamentStartRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sessionId !== "") {
      writer.uint32(10).string(message.sessionId);
    }
    if (message.roomId !== "") {
      writer.uint32(18).string(message.roomId);
    }
    if (message.tournamentId !== "") {
      writer.uint32(26).string(message.tournamentId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TournamentStartRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTournamentStartRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.roomId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.tournamentId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TournamentStartRequest {
    return {
      sessionId: isSet(object.sessionId) ? globalThis.String(object.sessionId) : "",
      roomId: isSet(object.roomId) ? globalThis.String(object.roomId) : "",
      tournamentId: isSet(object.tournamentId) ? globalThis.String(object.tournamentId) : "",
    };
  },

  toJSON(message: TournamentStartRequest): unknown {
    const obj: any = {};
    if (message.sessionId !== "") {
      obj.sessionId = message.sessionId;
    }
    if (message.roomId !== "") {
      obj.roomId = message.roomId;
    }
    if (message.tournamentId !== "") {
      obj.tournamentId = message.tournamentId;
    }
    return obj;
  },

  create(base?: DeepPartial<TournamentStartRequest>): TournamentStartRequest {
    return TournamentStartRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<TournamentStartRequest>): TournamentStartRequest {
    const message = createBaseTournamentStartRequest();
    message.sessionId = object.sessionId ?? "";
    message.roomId = object.roomId ?? "";
    message.tournamentId = object.tournamentId ?? "";
    return message;
  },
};

function createBaseTournamentStartResponse(): TournamentStartResponse {
  return { success: false, error: "" };
}

export const TournamentStartResponse: MessageFns<TournamentStartResponse> = {
  encode(message: TournamentStartResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.success !== false) {
      writer.uint32(8).bool(message.success);
    }
    if (message.error !== "") {
      writer.uint32(18).string(message.error);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TournamentStartResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTournamentStartResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.error = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TournamentStartResponse {
    return {
      success: isSet(object.success) ? globalThis.Boolean(object.success) : false,
      error: isSet(object.error) ? globalThis.String(object.error) : "",
    };
  },

  toJSON(message: TournamentStartResponse): unknown {
    const obj: any = {};
    if (message.success !== false) {
      obj.success = message.success;
    }
    if (message.error !== "") {
      obj.error = message.error;
    }
    return obj;
  },

  create(base?: DeepPartial<TournamentStartResponse>): TournamentStartResponse {
    return TournamentStartResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<TournamentStartResponse>): TournamentStartResponse {
    const message = createBaseTournamentStartResponse();
    message.success = object.success ?? false;
    message.error = object.error ?? "";
    return message;
  },
};

function createBaseTournamentQuitRequest(): TournamentQuitRequest {
  return { sessionId: "", tournamentId: "" };
}

export const TournamentQuitRequest: MessageFns<TournamentQuitRequest> = {
  encode(message: TournamentQuitRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sessionId !== "") {
      writer.uint32(10).string(message.sessionId);
    }
    if (message.tournamentId !== "") {
      writer.uint32(18).string(message.tournamentId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TournamentQuitRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTournamentQuitRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.tournamentId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TournamentQuitRequest {
    return {
      sessionId: isSet(object.sessionId) ? globalThis.String(object.sessionId) : "",
      tournamentId: isSet(object.tournamentId) ? globalThis.String(object.tournamentId) : "",
    };
  },

  toJSON(message: TournamentQuitRequest): unknown {
    const obj: any = {};
    if (message.sessionId !== "") {
      obj.sessionId = message.sessionId;
    }
    if (message.tournamentId !== "") {
      obj.tournamentId = message.tournamentId;
    }
    return obj;
  },

  create(base?: DeepPartial<TournamentQuitRequest>): TournamentQuitRequest {
    return TournamentQuitRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<TournamentQuitRequest>): TournamentQuitRequest {
    const message = createBaseTournamentQuitRequest();
    message.sessionId = object.sessionId ?? "";
    message.tournamentId = object.tournamentId ?? "";
    return message;
  },
};

function createBaseTournamentQuitResponse(): TournamentQuitResponse {
  return { success: false, error: "" };
}

export const TournamentQuitResponse: MessageFns<TournamentQuitResponse> = {
  encode(message: TournamentQuitResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.success !== false) {
      writer.uint32(8).bool(message.success);
    }
    if (message.error !== "") {
      writer.uint32(18).string(message.error);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TournamentQuitResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTournamentQuitResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.error = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TournamentQuitResponse {
    return {
      success: isSet(object.success) ? globalThis.Boolean(object.success) : false,
      error: isSet(object.error) ? globalThis.String(object.error) : "",
    };
  },

  toJSON(message: TournamentQuitResponse): unknown {
    const obj: any = {};
    if (message.success !== false) {
      obj.success = message.success;
    }
    if (message.error !== "") {
      obj.error = message.error;
    }
    return obj;
  },

  create(base?: DeepPartial<TournamentQuitResponse>): TournamentQuitResponse {
    return TournamentQuitResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<TournamentQuitResponse>): TournamentQuitResponse {
    const message = createBaseTournamentQuitResponse();
    message.success = object.success ?? false;
    message.error = object.error ?? "";
    return message;
  },
};

function createBaseTournamentFindByIdRequest(): TournamentFindByIdRequest {
  return { sessionId: "", tournamentId: "" };
}

export const TournamentFindByIdRequest: MessageFns<TournamentFindByIdRequest> = {
  encode(message: TournamentFindByIdRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sessionId !== "") {
      writer.uint32(10).string(message.sessionId);
    }
    if (message.tournamentId !== "") {
      writer.uint32(18).string(message.tournamentId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TournamentFindByIdRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTournamentFindByIdRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.tournamentId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TournamentFindByIdRequest {
    return {
      sessionId: isSet(object.sessionId) ? globalThis.String(object.sessionId) : "",
      tournamentId: isSet(object.tournamentId) ? globalThis.String(object.tournamentId) : "",
    };
  },

  toJSON(message: TournamentFindByIdRequest): unknown {
    const obj: any = {};
    if (message.sessionId !== "") {
      obj.sessionId = message.sessionId;
    }
    if (message.tournamentId !== "") {
      obj.tournamentId = message.tournamentId;
    }
    return obj;
  },

  create(base?: DeepPartial<TournamentFindByIdRequest>): TournamentFindByIdRequest {
    return TournamentFindByIdRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<TournamentFindByIdRequest>): TournamentFindByIdRequest {
    const message = createBaseTournamentFindByIdRequest();
    message.sessionId = object.sessionId ?? "";
    message.tournamentId = object.tournamentId ?? "";
    return message;
  },
};

function createBaseTournamentFindByIdResponse(): TournamentFindByIdResponse {
  return { tournament: undefined };
}

export const TournamentFindByIdResponse: MessageFns<TournamentFindByIdResponse> = {
  encode(message: TournamentFindByIdResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.tournament !== undefined) {
      TournamentView.encode(message.tournament, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TournamentFindByIdResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTournamentFindByIdResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.tournament = TournamentView.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TournamentFindByIdResponse {
    return { tournament: isSet(object.tournament) ? TournamentView.fromJSON(object.tournament) : undefined };
  },

  toJSON(message: TournamentFindByIdResponse): unknown {
    const obj: any = {};
    if (message.tournament !== undefined) {
      obj.tournament = TournamentView.toJSON(message.tournament);
    }
    return obj;
  },

  create(base?: DeepPartial<TournamentFindByIdResponse>): TournamentFindByIdResponse {
    return TournamentFindByIdResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<TournamentFindByIdResponse>): TournamentFindByIdResponse {
    const message = createBaseTournamentFindByIdResponse();
    message.tournament = (object.tournament !== undefined && object.tournament !== null)
      ? TournamentView.fromPartial(object.tournament)
      : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
